---
import {
  ImageField,
  Field,
  LinkField,
} from '@sitecore-jss/sitecore-jss-react';
import {
  Image,
  RichText,
} from "@astro-sitecore-jss/astro-sitecore-jss";

interface Fields {
  PromoIcon: ImageField;
  PromoText: Field<string>;
  PromoLink: LinkField;
  PromoText2: Field<string>;
}

type PromoProps = {
  params: { [key: string]: string };
  fields: Fields;
};

const props: PromoProps = Astro.props.route;
const id = props.params?.RenderingIdentifier;
---

<div
  class={`component promo ${props.params.styles}`}
  id={id ? id : undefined}
>
  <div class="component-content">
    <div class="field-promoicon">
      <Image field={props.fields.PromoIcon} />
    </div>
    <div class="promo-text">
      <div>
        <div class="field-promotext">
          <RichText class="promo-text" field={props.fields.PromoText} />
        </div>
      </div>
      <div class="field-promotext">
        <RichText class="promo-text" field={props.fields.PromoText2} />
      </div>
    </div>
  </div>
</div>
